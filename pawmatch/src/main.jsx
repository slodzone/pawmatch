import React from "react";
import { createRoot } from "react-dom/client";
import PawMatch from "./PawMatch.jsx";

createRoot(document.getElementById("root")).render(
  <React.StrictMode>
    <PawMatch />
  </React.StrictMode>
);
